---
description: Default git push goes to test; main only when the user explicitly asks
alwaysApply: true
---

# Git 推送目标

默认远程分支是 `test`，不是 `main` / `master`。

- 用户说「提交」「推送」「同步仓库」、或只说 push，**一律** `git push origin HEAD:test`（或当前分支跟踪 `origin/test`）。
- **禁止** `git push origin main`、`git push origin master`、`--force` 推主分支。
- 只有用户明确说「推送到主分支」「推 main」「合并到 main」时，才 `git push origin HEAD:main`。
- 「提交」若同时要推送：先 commit，再推 `test`。不要顺便推 `main`。
- 新建功能分支也可以，但默认上游仍是 `test`，不要把 `main` 设成默认跟踪分支。
