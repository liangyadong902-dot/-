const api=require('../../services/api');Page({data:{list:[],loading:true,category:'all'},onLoad(){this.load()},async load(){this.setData({loading:true});try{const list=await api.listBoxes({category:this.data.category});this.setData({list:list||[],loading:false})}catch(e){this.setData({loading:false})}},filter(e){this.setData({category:e.currentTarget.dataset.category},()=>this.load())},open(e){wx.navigateTo({url:'/pages/product/detail?id='+e.currentTarget.dataset.id})}})
