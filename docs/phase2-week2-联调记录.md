# 第二阶段第二周联调记录

> 日期：2026-09-18  
> 范围：内容管理端第二批交互、线路池校验、权限与构建回归

## 已完成

- 盲盒管理：真实新增、编辑、上下架、删除、线路池抽屉。
- 线路管理：真实新增、编辑、启停、删除；保存采购价和场景字段。
- 徽章管理：真实新增、编辑；展示解锁人数和关联线路数。
- Banner 管理：真实新增、编辑、上下架、删除；支持跳转类型、时间窗口和排序。
- 线路池抽屉展示全部候选线路、满足保底标记和 `ok` 结果。
- 管理端列表数据全部来自 `/api/v1/admin/*`，保存后重新请求后端刷新。
- 小程序旧版首页 Store 接入真实盲盒、Banner、徽章接口；筛选请求携带分类与心情参数。
- 图鉴接口按 `{ total, list }` 解析，游客状态不再用本地徽章种子覆盖服务端结果。
- 社区保留在 5 项 TabBar，图鉴从首页入口通过页面导航打开。

## 已验证

| 项目 | 结果 |
| --- | --- |
| 管理端 `npm run build` | 通过 |
| 服务端 `mvn test -q` | 通过 |
| 管理端盲盒分页 | 返回真实数据库数据 |
| 线路池 `GET /api/v1/admin/boxes/2/pool` | 返回候选线路和 `ok=true` |
| 徽章分页 | 返回总数 12、解锁人数和线路数 |
| Banner 分页 | 返回有效期、跳转类型和上下架状态 |
| 不存在账号登录 | `401 账号或密码错误` |
| `cs` 角色写内容 | `403 当前角色无内容编辑权限` |

## 待完成

- 使用微信开发者工具固定 375px 视口，对首页、图鉴页和盲盒预览弹窗完成截图对照。
- 在真实管理端操作一次价格修改和下架，确认小程序刷新后的端到端结果。
- 将截图、差异说明和修复后截图归档到本目录。

## 已知边界

- 社区保留为当前 5 项 TabBar，图鉴页面仍保留为可访问页面，但不占用 TabBar 槽位。
- 订单、退款、用户、AI 等非第二阶段内容域页面仍等待对应阶段接口，不作为本阶段内容配置验收项。
